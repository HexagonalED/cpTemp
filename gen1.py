import random as rand


def gen(n) : 
    for total in range(0,n):
        size = rand.randrange(1,5001);
        fin = open("./input/{}.in",format(total+1),'w')
        fout = open("./output/{}.in",format(total+1),'w')
        for


